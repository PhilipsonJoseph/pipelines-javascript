trigger: none
jobs:
- deployment: deploymentJob1
  environment: testEnvironmentCheck.testAgent1
  pool: Default
  strategy:
   runOnce:
    deploy:
     steps:
     - script: echo Hello, world!
       displayName: 'Run a one-line script'